import type { Concept } from "../types";

function cx(
  id: string,
  title: string,
  simple: string,
  technical: string,
  analogy: string,
  command: string,
  output: string,
  explanation: string,
  q: string,
  a: string,
  related: string[],
  takeaway: string,
): Concept {
  return {
    id,
    title,
    simple,
    technical,
    analogy,
    example: { command, output, explanation },
    mistakes: ["Skipping the simple model and jumping to flags.", "Copying a command without knowing which process it changes."],
    practices: ["Explain it out loud in one sentence before you run it.", "Check man or --help for the exact flag dialect (GNU vs BSD)."],
    exercise: {
      prompt: `After reading “${title}”, write one sentence you could tell a junior teammate.`,
      solution: takeaway,
    },
    interview: { question: q, answer: a },
    related,
    takeaway,
  };
}

export const extraConceptsMore: Record<string, Concept[]> = {
  architecture: [
    cx("syscalls-plain", "System calls are the only legal door", "Programs cannot poke the disk themselves. They ask the kernel through a small list of requests: open, read, write, fork, mmap, connect.", "glibc wraps those requests. strace prints them. Section 2 of the manual is the syscall list. Root is still a user-space identity.", "A hotel guest (app) fills a request form (syscall). Staff (kernel) enter the service corridors.", "man 2 syscalls | head", "syscalls(2)", "You do not memorize every syscall. You remember that Permission denied is usually the kernel saying no.", "Root vs kernel mode?", "Root is uid 0 in user space. Kernel mode is a CPU privilege level. Root still uses syscalls.", ["process-model"], "If it needs hardware or isolation, it goes through a syscall."),
    cx("shared-libs", "Why ldd matters", "Most programs share C libraries. If libc is missing, the binary will not start even if the file is on disk.", "ldd shows dynamic deps. Alpine musl cannot run a glibc binary. Static binaries skip this but are larger.", "A lamp that needs a specific wall socket.", "ldd /bin/ls | head", "linux-vdso.so.1 ... libc.so.6 => /lib/...", "This is why 'it works on my Ubuntu' fails in Alpine.", "What is a missing shared library error?", "The dynamic linker cannot find a .so the binary was built against. Install the package or rebuild for that libc.", ["package-managers"], "Match libc (glibc vs musl) to the binary."),
  ],
  "boot-and-init": [
    cx("grub-plain", "GRUB is the chooser", "Before Linux runs, firmware starts a bootloader. GRUB lets you pick a kernel. A bad kernel update is often fixed by booting the previous entry.", "UEFI → EFI binary or GRUB → vmlinuz + initrd. Kernel cmdline lives in grub config or boot loader entries.", "A receptionist who decides which manager starts the shift.", "ls /boot | head", "vmlinuz-...\ninitrd.img-...", "Those two files are the kernel and its first-aid kit (initramfs).", "How do you boot an old kernel?", "From the GRUB menu pick the previous vmlinuz. Cloud serial console if you have no screen.", ["systemd"], "Keep one known-good kernel; GRUB is your undo."),
    cx("initramfs-plain", "initramfs is a tiny Linux for finding the disk", "The real root disk may be encrypted, LVM, or on the network. initramfs is a small filesystem in RAM that finds it, then switches to the real /.", "If initramfs lacks a driver, you get an emergency shell. Rebuilding initramfs after a driver change is a real ops task.", "A flashlight you use to find the light switch.", "lsinitramfs /boot/initrd.img-$(uname -r) 2>/dev/null | head || echo 'tool optional'", "(module list)", "You are looking for disk and crypto modules.", "Why does dropping to initramfs happen?", "Root was not found: wrong UUID, missing module, LVM not started, or encrypt password.", ["disks-partitions"], "Boot failures before a login prompt are firmware, GRUB, kernel, or initramfs — not Bash."),
  ],
  "directory-map": [
    cx("where-to-put-files", "Where things actually go", "Config: /etc. Logs and changing data: /var. Your work: /home. Programs the distro shipped: /usr. Temporary: /tmp or /run.", "FHS is the contract. Merged /usr means /bin is a symlink to /usr/bin. Do not store app state in /usr — upgrades wipe the idea of 'I installed it here forever'.", "A labeled warehouse.", "ls -ld /etc /var /usr /home /tmp /run", "drwxr-xr-x ...", "Say the purpose of each before you cd.", "Logs in /usr — why is that wrong?", "/usr is for shipped software; logs grow and belong in /var so you can mount it separately.", ["fhs-deep-dive"], "If it changes while the app runs, it is probably /var, /home, or /run — not /usr."),
    cx("tmp-vs-run", "/tmp versus /run", "/tmp is shared scratch and may persist across reboots on some systems. /run is a RAM filesystem for PID files and sockets this boot.", "systemd-tmpfiles cleans /tmp. Putting secrets in /tmp is a classic leak. /dev/shm is another tmpfs.", "Sticky notes on a fridge (/tmp) vs a whiteboard erased every morning (/run).", "df -h /tmp /run 2>/dev/null | cat", "tmpfs ... /run", "/run filling up can break services (no room for sockets).", "Why sticky bit on /tmp?", "World-writable, but you can only delete your own files.", ["special-bits-acl"], "Never put passwords in /tmp."),
  ],
  "help-systems": [
    cx("how-to-read-man", "How to actually read a man page", "Start at SYNOPSIS, then DESCRIPTION, then skip to EXAMPLES or SEE ALSO. /search works in less. q quits.", "Sections: 1 commands, 2 syscalls, 5 file formats, 8 admin. man 5 passwd is not the passwd command.", "A book with numbered wings.", "man man", "MAN(1)", "Learning the pager is a real skill.", "man -k vs apropos?", "They search the whatis database by keyword when you forgot the command name.", ["bash-basics"], "Professionals answer flag questions with man, not a random blog."),
    cx("help-flags", "--help, info, and tldr", "GNU programs print --help. info is a hyperlinked GNU manual. Community tldr pages are short; still verify against man for production.", "Bash builtins use help cd. type pwd tells you if it is builtin or a disk binary.", "A restaurant: --help is the daily specials; man is the full menu.", "type pwd; help pwd | head", "pwd is a shell builtin", "Builtins do not have the same flags as /bin/pwd.", "Why does man pwd differ from help pwd?", "help documents the Bash builtin; man 1 pwd documents the standalone binary.", ["env-and-path"], "Always ask: builtin or external binary?"),
  ],
  "terminal-shell": [
    cx("ctrl-keys", "Ctrl-C, Ctrl-Z, Ctrl-D", "Ctrl-C sends SIGINT (interrupt). Ctrl-Z stops the job. Ctrl-D sends EOF (end of input), it is not 'kill'.", "The terminal driver translates keys into signals or bytes. SSH uses a pty so remote programs see the same thing.", "A doorbell (signal) vs hanging up the phone (EOF).", "stty -a | head", "intr = ^C; eof = ^D; susp = ^Z", "You can remap them; do not, until you know why.", "Does Ctrl-C kill daemons?", "Only the foreground process group of that terminal. Daemons usually have no controlling tty.", ["jobs-signals"], "Ctrl-C is a request, not SIGKILL."),
    cx("tmux-later", "Why people use tmux", "A multiplexer keeps shells alive if SSH drops and splits the screen. Learn it after you are fluent in one pane.", "tmux/screen attach to a session. systemd lingering and mosh are related ideas.", "A desk that stays tidy when you leave the room.", "tmux -V 2>/dev/null || echo 'install later'", "tmux 3.x", "Optional on servers; invaluable on long jobs.", "tmux vs nohup?", "tmux is an interactive session. nohup/disown/systemd are for detaching one process.", ["process-model"], "Do not start with tmux on day one — but know it exists for remote work."),
  ],
  "bash-basics": [
    cx("exit-codes", "Zero means success", "Every program returns a number. 0 means success. Anything else means failure. echo $? prints the last one.", "Pipelines: only the last command's status unless set -o pipefail. 127 is command not found. 126 is not executable.", "A stamp on homework: 0 is pass.", "false; echo $?", "1", "false is a real command whose job is to fail.", "Why check $? in scripts?", "To stop on failure instead of charging ahead with a missing file.", ["bash-scripting"], "Always know whether the last command succeeded."),
    cx("type-command", "How the shell finds ls", "The shell checks aliases, functions, builtins, then PATH. type ls shows which one you will run.", "hash caches the path. After an install, hash -r. command ls skips aliases.", "Looking through labeled drawers in order.", "type ls; command -v ls", "ls is hashed (/usr/bin/ls)", "command -v is the portable cousin.", "alias vs PATH?", "Aliases are expanded first in interactive shells and usually not in scripts unless you shopt.", ["env-and-path"], "When the 'wrong ls' runs, type is the first debug tool."),
  ],
  "env-and-path": [
    cx("export-plain", "export makes children see it", "A variable is local to the shell unless you export it. Child programs only inherit exported names.", "VAR=value cmd runs one command with an extra variable. env -i starts a clean environment — that is why cron surprises you.", "A note on your desk vs a memo sent to the next room.", "export | head", "declare -x PATH=...", "PATH is exported so every command can find binaries.", "Why did my script not see MYVAR?", "You set it in an interactive shell without export, or the script is a new process with a fresh environment.", ["quoting-expansion"], "If a child must see it, export it — or pass it on the command line."),
    cx("dot-on-path", "Never put . on PATH", "An empty PATH component or :. means 'current directory'. A file named ls in a download folder could then run instead of /bin/ls.", "This is a classic privilege trick. Cron with a short PATH is safer than a clever PATH.", "Leaving the spare key under the doormat.", "echo \"$PATH\"", "/usr/local/bin:/usr/bin:/bin", "No empty :: segments, no .", "How do you run a script in . safely?", "./script.sh — the slash means 'this path', not PATH lookup.", ["linux-security"], "Type ./ for local scripts. Keep PATH to trusted directories."),
  ],
  "quoting-expansion": [
    cx("glob-plain", "Globs happen before the program runs", "Unquoted * is replaced by filenames by the shell. The program never sees the star.", "set -f disables globbing. nullglob changes empty-match behavior in Bash.", "You unpack a box (glob) before handing items to a worker (the command).", "echo *", "(filenames)", "echo '*' prints a star.", "Why did rm * delete more than you thought?", "The shell expanded * to every name, including ones you did not look at.", ["practical-scripts"], "Quote variables. Only glob when you mean 'all these files'."),
    cx("word-split", "Word splitting is the silent bug", "Unquoted $files splits on spaces (IFS). A filename with a space becomes two arguments.", "IFS newline hacks exist; arrays are better in Bash. POSIX: quote \"$@\".", "A sentence ripped at every space.", "name='a b'; printf '%s\\n' $name", "a\nb", "Quoted \"$name\" stays one word.", "[ $x = 1 ] without quotes?", "If x is empty, test sees too few arguments and errors. Always [ \"$x\" = 1 ].", ["bash-scripting"], "Quote variables by default. Unquoted is the exception you can explain."),
  ],
  "inodes-and-links": [
    cx("hard-vs-soft", "Hard link vs symlink — the interview pair", "A hard link is another name for the same inode (same data). A symlink is a tiny file that stores a path.", "Hard links cannot cross filesystems and cannot usually name directories. rm of one name does not delete data until the last link (and open fds) are gone.", "Two catalog cards for one book (hard) vs a sticky note saying 'see aisle 4' (soft).", "man ln | head", "LN(1)", "ln file hard; ln -s file soft", "Which survives if you rename the target?", "A hard link still points at the inode. A relative symlink may break.", ["mounts"], "Same inode number = hard link. A path string = symlink."),
    cx("deleted-but-open", "Why df stays full after rm", "rm unlinks the name. If a process still has the file open, the kernel keeps the data until close.", "lsof +L1 or lsof | grep deleted. Truncate via /proc/pid/fd/N or restart the daemon.", "Throwing away the label but leaving the box in someone's hands.", "man lsof | head", "LSOF(8)", "This is the #1 disk-full surprise.", "How do you free a deleted log?", "Restart the writer, or truncate the still-open fd after confirming it is the right process.", ["process-control"], "Unlink ≠ free space if something still has the file open."),
  ],
  mounts: [
    cx("fstab-plain", "fstab is the boot packing list", "Each line is: what to mount, where, type, options, dump, fsck pass. Use UUID= so device names can change.", "nofail for optional disks. noexec on /tmp is a hardening choice. bind mounts remount a tree elsewhere.", "A seating chart for filesystems.", "findmnt --fstab 2>/dev/null | head || cat /etc/fstab | head", "UUID=... / ext4 ...", "Never copy /dev/sda from a blog onto NVMe.", "What is a bind mount?", "The same tree visible at a second path. Used in containers and chroots.", ["lvm-and-fstab"], "Identify with findmnt before umount. Persist with UUID in fstab."),
    cx("busy-umount", "Why umount says busy", "A process has cwd, open files, or a mount namespace on that filesystem.", "lsof +f -- /mnt or fuser -vm. lazy umount (-l) detaches the name but waits for references — use with care.", "Trying to leave a room while people are still inside.", "findmnt /", "/ /dev/... ext4", "Know the mount before you blame the app.", "lazy umount risk?", "Processes keep using the old fs; surprises later. Prefer stop-the-app then umount.", ["process-model"], "BUSY means references. Find them; do not reboot first."),
  ],
  "fhs-deep-dive": [
    cx("proc-self", "/proc/self is you", "/proc/self is a symlink to the process reading it. /proc/PID/fd lists file descriptors. /proc/PID/cwd is the working directory.", "This is how lsof and ss learn so much. It is not a disk you back up.", "A name tag that always says 'me'.", "ls -l /proc/self/fd", "0 -> /dev/pts/0 ...", "0,1,2 are stdin, stdout, stderr.", "Can you cd into /proc/1/root?", "With enough privilege that is the mount namespace root of PID 1 — a container-host debug trick.", ["namespaces-cgroups"], "/proc is a live API, not documents."),
    cx("sys-vs-proc", "/sys versus /proc", "/proc is process-centric and some sysctls. /sys is a tree of devices and drivers (sysfs). Writing the wrong file can change hardware state.", "udev uses /sys. Do not chmod randomly here.", " /proc is HR files; /sys is the building wiring diagram.", "ls /sys/class | head", "block  net  ...", "Read-only curiosity is fine; writes are configuration.", "sysctl vs /proc/sys?", "sysctl is the friendly interface to /proc/sys knobs.", ["architecture"], "Look in /sys for devices, /proc for processes and some tunables."),
  ],
  "users-groups": [
    cx("sudo-vs-su", "sudo versus su", "su switches user (often to root) and typically needs the target password. sudo runs one command as another user using your password and a policy file.", "visudo edits /etc/sudoers. NOPASSWD is convenient and dangerous. sudo -i is a root login shell.", "A guest badge for one room (sudo) vs taking the master key (su).", "sudo -l | head", "(policy)", "See what you are allowed to run.", "Why visudo?", "It syntax-checks sudoers so you cannot lock yourself out with a typo as easily.", ["linux-security"], "Prefer sudo with a tight command list over sharing the root password."),
    cx("nsswitch-plain", "passwd file is not always the whole story", "getent passwd user uses NSS: files, then maybe sssd/LDAP. grep /etc/passwd misses directory users.", "Cloud images may use none of this and rely on cloud-init users.", "A front desk that checks local roster then HQ.", "getent passwd \"$USER\"", "student:x:1000:1000:...", "Use getent in scripts.", "What is the x in passwd?", "Password hash lives in /etc/shadow, not in the world-readable passwd file.", ["mode-bits"], "Identity is UID. Names are labels. NSS may come from the network."),
  ],
  "mode-bits": [
    cx("directory-x", "Execute on a directory means enter", "Read on a directory lists names. Execute (search) lets you walk into it and resolve a file by name. Write lets you create/delete names.", "A file 400 with a directory 100 can still be opened if you know the name — until you also need to list. Interviews love this.", "A hallway light (x) vs a directory sign (r).", "man chmod | head", "CHMOD(1)", "Symbolic u+x is safer than 777.", "Why 777 is a bad fix?", "Anyone can replace binaries, dump secrets, or implant scripts. Fix the owner/group/mode instead.", ["special-bits-acl"], "For directories, x is 'can I go in?', not 'can I run a program'."),
    cx("umask-plain", "umask is the subtraction", "New files are 666 minus umask; new dirs 777 minus umask. umask 022 → files 644. umask 077 → files 600.", "Processes inherit umask. Shared-dir workflows use 002 plus setgid.", "A cookie cutter that removes bits before the file is born.", "umask", "0022", "Leading 0 is octal.", "Why did my service create 666 files?", "It started with umask 000. Set UMask= in systemd or call umask in the unit.", ["systemd"], "If new files are too open, look at umask before blaming chmod later."),
  ],
  "special-bits-acl": [
    cx("setuid-plain", "setuid in one sentence", "An executable with setuid runs as the file owner, not as you. passwd is the classic example. Linux ignores setuid on scripts.", "Attackers love writable setuid binaries. Prefer capabilities or sudo.", "A sealed company stamp the program may use.", "ls -l /usr/bin/passwd", "-rwsr-xr-x ...", "s in the owner x slot is setuid.", "Why not setuid bash?", "That would give a root shell to anyone who can run it. Distros do not ship that.", ["linux-security"], "setuid is a sharp tool. Inventory it; do not add it casually."),
    cx("acl-mask", "ACLs when owner/group/other is not enough", "ACL adds named users. The mask caps group-class permissions. getfacl/setfacl. A '+' in ls -l hints an ACL.", "NFS and some backup tools mishandle ACLs. Know if you rely on them.", "Extra names on the guest list.", "getfacl . | head", "# file: .", "If getfacl is missing, install acl.", "ls -l plus sign?", "Often means an ACL (or selinux, depending on context — check getfacl and ls -Z).", ["users-groups"], "If 640 is not enough granularity, ACLs — or better group design."),
  ],
  "process-model": [
    cx("fork-exec-plain", "fork then exec — how commands start", "The shell copies itself (fork), then the child replaces itself with ls (exec). The parent wait()s if the job is foreground.", "PID stays the same across exec. File descriptors stay unless O_CLOEXEC. This is why 'cd in a script' cannot change your interactive cwd: the script is another process.", "Photocopy a waiter, then the copy puts on a chef hat.", "ps -o pid,ppid,cmd | head", "PID PPID CMD", "Your shell's parent is often systemd or tmux.", "What does exec not change?", "PID, most fds, and credentials (unless the binary is setuid).", ["jobs-signals"], "Starting a program is almost always fork+exec, not 'the kernel typing'."),
    cx("threads-plain", "Threads share memory; processes usually do not", "A thread is a task that shares the address space. ps -L or top H shows them. Java and Node apps are thread-heavy.", "Linux implements threads as tasks with the same VM. kill a PID kills the thread group.", "Roommates (threads) vs separate apartments (processes).", "ps -o pid,nlwp,cmd | head", "NLWP is thread count.", "High NLWP is not always bad.", "fork vs clone?", "clone is the Linux syscall underneath; flags choose what to share (threads share VM).", ["namespaces-cgroups"], "CPU% in top may be one thread. Look at the whole process too."),
  ],
  "jobs-signals": [
    cx("term-then-kill", "TERM, wait, then KILL", "SIGTERM asks the process to exit and clean up. SIGKILL cannot be caught. Always try TERM first.", "systemd TimeoutStopSec then SIGKILL. kill -9 is not faster if the process is in D state.", "An eviction notice versus demolishing the building.", "kill -l | tr ' ' '\\n' | head", "SIGHUP SIGINT ... SIGKILL SIGTERM", "15 is TERM, 9 is KILL.", "Can you trap SIGKILL?", "No.", ["process-control"], "Polite stop first. KILL is last resort, not first muscle memory."),
    cx("nohup-disown", "HUP, nohup, and disown", "SIGHUP historically meant the terminal hung up. SSH drop can SIGHUP jobs. nohup ignores HUP. disown removes a job from the shell's table.", "systemd user lingering and tmux survive disconnects more cleanly for interactive work.", "Leaving a slow cooker on when you leave the house.", "man nohup | head", "NOHUP(1)", "Prefer systemd services for real daemons.", "Why did my SSH job die when I disconnected?", "The shell got HUP and forwarded it. Use tmux, systemd, or nohup.", ["systemd"], "Long jobs belong in a service or multiplexer, not a raw SSH fork."),
  ],
  "process-control": [
    cx("nice-ionice", "nice is CPU politeness, not disk", "nice +10 means 'I can wait'. It does not slow a single-threaded CPU hog much on an idle machine. ionice is for I/O class.", "Priority is 20+nice. Only root can nice below 0 on typical configs.", "Letting others cut in line.", "nice -n 10 echo ok", "ok", "Start long compiles nicer.", "renice a running PID?", "renice -n 10 -p PID", ["performance-basics"], "Measure with top/vmstat before you nice. Disk issues need iostat, not nice."),
    cx("zombie-parent", "Fix the parent, not the zombie", "Zombies only hold an exit code. The parent must wait(). Killing the zombie PID does nothing useful.", "If PID 1 is your app in a container, it must reap or you need a tiny init (tini).", "Closed tickets stacked on a manager's desk.", "ps -eo stat,pid,ppid,cmd | awk '$1 ~ /Z/'", "(maybe empty)", "A few fleeting Z are normal.", "Orphan vs zombie?", "Orphan is alive, reparented. Zombie is dead, waiting for wait().", ["namespaces-cgroups"], "Piles of Z mean a broken parent, often in containers."),
  ],
  "net-fundamentals": [
    cx("localhost-bind", "127.0.0.1 versus 0.0.0.0", "Listen on 127.0.0.1: only this machine. 0.0.0.0: every IPv4 interface. That one mistake publishes a debug server to the world.", "ss -tlnp shows the bind address. Firewalls still apply.", "A shop with a back-room phone versus a sign on the street.", "ss -tln | head", "LISTEN ... 127.0.0.1:631", "Local-only is a feature.", "ping works, app does not?", "ICMP is not TCP. Check the port with ss/curl.", ["tcp-and-sockets"], "Always read the listen address before you celebrate 'it started'."),
    cx("tcp-udp-plain", "TCP is a call; UDP is a postcard", "TCP: handshake, order, retries. UDP: send and hope; DNS and many games use it. QUIC/HTTP3 rebuild reliability on UDP.", "Connection refused = RST, host reached, no listener. Timeout = drop or blackhole.", "Phone call vs postcard.", "man 7 ip | head", "IP(7)", "Keep the model; details come with ss -tmi.", "Why is DNS often UDP/53?", "Small queries fit one datagram. TCP/53 is used for large responses and zone transfers.", ["name-resolution"], "Ping ≠ open port. Refused ≠ timeout."),
  ],
  "name-resolution": [
    cx("hosts-then-dns", "/etc/hosts is the first cheat sheet", "A static name→IP mapping. Useful for labs; dangerous if you forget it in production.", "nsswitch.conf order: files dns. systemd-resolved adds a stub at 127.0.0.53.", "A paper address book versus the phone directory.", "getent hosts localhost", "127.0.0.1 localhost", "getent uses NSS, dig does not.", "dig works, curl does not resolve?", "dig talks to a DNS server directly. curl uses NSS (stub, search domains, mdns).", ["remote-access"], "Debug with getent first, then dig, then resolvectl."),
    cx("default-route", "The default route is 'how packets leave'", "ip route shows via and dev. No default route = you can talk LAN but not the internet.", "Cloud metadata routes are extra. Wrong route is a silent outage.", "The 'exit' sign in a building.", "ip -br a; ip r", "lo UP ...\ndefault via ...", "Learn ip, not ifconfig, on modern Linux.", "DHCP vs static?", "DHCP leases address, route, and DNS. Static is pinned; document it.", ["net-fundamentals"], "If names fail, check DNS. If IPs fail, check route and firewall."),
  ],
  "remote-access": [
    cx("keys-not-passwords", "Keys beat passwords", "ssh-keygen -t ed25519. Private key 600. Public key in authorized_keys. Disable PasswordAuthentication once keys work.", "ssh-copy-id installs the public key. ssh -vvv shows which key is offered. IdentitiesOnly yes stops spraying every key.", "A badge, not a shouted password in the hallway.", "man ssh-keygen | head", "SSH-KEYGEN(1)", "Never paste a private key into chat.", "What does host key checking stop?", "MITM: a fake server stealing credentials.", ["linux-security"], "Mode 700 on ~/.ssh, 600 on the private key, StrictModes on the server."),
    cx("proxyjump", "Jump hosts without Swiss-cheese VPNs", "ssh -J bastion target or ProxyJump in ssh_config. Agent forwarding is convenient and risky; prefer ProxyJump.", "scp -o ProxyJump=bastion. Capital P for scp ports.", "A locked lobby before the office floor.", "man ssh_config | head", "ssh_config(5)", "Config beats 12 flags you will forget.", "Why not Disable StrictHostKeyChecking?", "You silently accept impostors.", ["host-config"], "One bastion, key auth, and a config file scale better than a mesh of passwords."),
  ],
  "package-managers": [
    cx("update-then-install", "Update the index, then install", "apt update refreshes metadata. install without update can try to fetch vanished URLs. dnf/yum similar.", "Pin versions in production images. Mixing pip --break-system-packages with apt Python is how boxes rot.", "Check the catalog, then order.", "man 8 apt | head || man dnf | head", "APT(8)", "Use the local family's man page.", "dpkg vs apt?", "dpkg unpacks a .deb you already have. apt finds, downloads, and resolves deps, then calls dpkg.", ["production-linux"], "Never wget a random .deb onto a production host."),
    cx("hold-pin", "Holds and pins", "apt-mark hold freezes a package. dnf versionlock similar. Use when a kernel or glibc upgrade must wait.", "Document why the hold exists or you will debug ghosts in a year.", "A 'do not restock' tag on a shelf.", "apt-mark showhold 2>/dev/null || echo 'RPM family uses versionlock'", "", "Empty hold list is normal.", "Partial upgrade risk on Debian?", "Mixing old and new libs. Prefer full-upgrade in a window, or immutable images.", ["host-config"], "If you freeze a package, write down the ticket number."),
  ],
  "bash-scripting": [
    cx("shebang", "The shebang chooses the interpreter", "#!/usr/bin/env bash finds bash on PATH. #!/bin/sh is POSIX (often dash). The kernel reads the first line.", "Windows CRLF breaks shebangs. chmod +x is required to exec the file; bash file.sh does not need +x.", "A note on the script saying which chef to call.", "head -n 1 /bin/true 2>/dev/null || echo 'binary — scripts start with #'", "", "Your scripts should start with #!/usr/bin/env bash or sh.", "Why env bash?", "bash is not always in /bin (Nix, some BSDs).", ["quoting-expansion"], "Match the shebang to the language you actually used (no Bash arrays in sh)."),
    cx("set-e-pitfalls", "set -e is help, not a proof", "set -e exits on failure. It does not fire in some if tests. Pipelines need pipefail. Critical commands still deserve explicit checks.", "shellcheck catches a lot. Do not ignore SC warnings you do not understand.", "A seatbelt, not a helmet.", "help set | head", "set: set [--abefhkmnptuvxBCEHPT] ...", "Read set -e, -u, -o pipefail together.", "When is set -e skipped?", "In the condition of if, while, and some &&/|| lists — read the Bash manual before you trust it.", ["practical-scripts"], "Quote, check, and shellcheck. set -euo pipefail is the start, not the finish."),
  ],
  "pipes-redirection": [
    cx("order-of-redir", "Redirection order is the trick question", ">f 2>&1 sends both stdout and stderr to the file. 2>&1 >f leaves stderr on the terminal.", "Left to right. 2>&1 copies the current stdout.", "Plumbing valves in sequence.", "echo hi > /tmp/out 2>&1", "", "Both streams follow stdout into the file.", "What does | do to PIDs?", "Two processes; kernel buffer between them. Writers block when full.", ["bash-scripting"], "Say the redirections out loud left to right before you press enter."),
    cx("tee-and-pipefail", "tee and hidden failures", "cmd | tee log.txt prints and saves. Without pipefail, a failing cmd still looks like success if tee succeeds.", "Use set -o pipefail or PIPESTATUS.", "A T-junction in a pipe.", "echo hello | tee /tmp/t | cat", "hello", "tee is not a backup system.", "When is a pipeline's exit status 0 despite a fail?", "Default: last command. grep finding nothing is status 1 — that surprises CI.", ["practical-scripts"], "If a pipeline matters, enable pipefail or check PIPESTATUS."),
  ],
  "practical-scripts": [
    cx("find-print-first", "find: print before delete", "Always find ... -print and read the list. Then -delete or -exec. Never start with -delete copied from a chat.", "-print0 | xargs -0 for spaces. -xdev stays on one filesystem.", "Look in the fridge before throwing food.", "find . -name '*.tmp' -print | head", "(paths)", "Preview is the professional step.", "Why xargs without -0 is unsafe?", "Spaces and quotes split names. -print0 / -0 is the contract.", ["quoting-expansion"], "Preview, then mutate. Especially with find and rm."),
    cx("cron-path", "Cron has a tiny PATH", "Your aliases and nvm PATH are gone. Use full paths or set PATH at the top of the crontab.", "Redirect stdout/err to a log. systemd timers are easier to debug with journalctl.", "A night shift worker who does not know your desk layout.", "crontab -l 2>/dev/null || echo 'no user crontab'", "", "Empty is fine.", "Why did cron skip my script?", "Missing shebang, CRLF, PATH, cwd, or mail of errors you never read.", ["logs-and-jobs"], "Write cron jobs as if the environment is empty — because it is."),
  ],
  systemd: [
    cx("enable-vs-start", "enable versus start", "start is this boot. enable creates the wants/ symlink for future boots. enable --now does both. disable + stop is the reverse.", "systemctl cat shows the merged unit. systemctl edit writes a drop-in so upgrades do not wipe you.", "Hiring for today vs putting them on the permanent roster.", "systemctl is-enabled ssh 2>/dev/null || systemctl is-enabled sshd", "enabled", "Names differ by distro: ssh vs sshd.", "Where must you not edit?", "/lib/systemd/system or /usr/lib — package files. Use drop-ins in /etc.", ["logs-and-jobs"], "Drop-ins over rewriting vendor units."),
    cx("type-simple", "Type=simple versus forking", "simple: ExecStart is the main process (the default). forking: the parent daemonizes and systemd tracks the child. Wrong Type= means 'started' but immediately 'dead'.", "notify and Type=exec are modern. Read the unit before you guess.", "A manager who stays on the floor versus one who hires someone and leaves.", "man systemd.service | head", "SYSTEMD.SERVICE(5)", "This page is worth a slow read once.", "Restart=on-failure loop?", "Crash loop. Check journalctl -u and ExecStart path.", ["process-model"], "Match Type= to how the binary actually lives."),
  ],
  "logs-and-jobs": [
    cx("journal-boot", "journalctl -b is this boot", "-u unit, -f follow, -p err priority, --since today. Persistent journals live in /var/log/journal.", "Rate limits hide floods. vacuum if journals eat the disk.", "A black box recorder with filters.", "journalctl -b -p err --no-pager | tail", "(errors)", "Empty is a good day.", "syslog files vs journal?", "rsyslog may still write /var/log/syslog. Distros often run both.", ["host-config"], "Start with journalctl -u name -b before you grep random files."),
    cx("timers-vs-cron", "Timers versus cron", "A .timer unit with OnCalendar= plus a matching .service. Persistent=true catches up after downtime. Logs land in the journal.", "cron is fine and portable. New work on systemd hosts is often easier as a timer.", "A calendar invite systemd honors.", "systemctl list-timers --all | head", "NEXT LEFT LAST UNIT", "If the table is empty, no timers.", "OnCalendar daily 01:30?", "*-*-* 01:30:00 in the timer unit.", ["practical-scripts"], "Prefer timers when you already live in systemd; keep cron for portable one-liners."),
  ],
  "host-config": [
    cx("ntp-tls", "Wrong time breaks TLS", "Certificates are only valid in a time window. NTP/chrony/timedatectl must be active. Servers should use UTC.", "timedatectl set-ntp true. chronyc tracking on some distros.", "A clock that is 3 days off will reject 'today's' tickets.", "timedatectl | head", "NTP service: active", "If inactive, fix time before chasing 'random' curl errors.", "Why UTC in logs?", "No DST jumps; correlate fleets; convert in the UI.", ["linux-security"], "Time sync is a security control, not a nicety."),
    cx("sshd-t", "sshd -t before reload", "A bad sshd_config can lock you out. Test, then reload, with a second session still open.", "PasswordAuthentication no, PermitRootLogin no, AllowUsers. Cloud security groups still need 22 or your bastion port.", "Proofread the lock before you close the door.", "man sshd_config | head", "SSHD_CONFIG(5)", "The real flag list.", "PermitRootLogin prohibit-password?", "Root may use keys, not passwords — still better to forbid root SSH and sudo from a user.", ["remote-access"], "Never restart sshd as the last open session without -t and a backup login."),
  ],
  "disks-partitions": [
    cx("lsblk-first", "lsblk is the pause button", "Before fdisk or mkfs, lsblk -f: size, TYPE, FSTYPE, UUID, MOUNTPOINT. If it is mounted, you are about to destroy data.", "NVMe is nvme0n1p2, not sda. Virtio is vda. Tutorials lie.", "Look at the warehouse map before you paint.", "lsblk -o NAME,SIZE,TYPE,FSTYPE,MOUNTPOINT", "(table)", "MATCH the device you think you have.", "GPT vs MBR in one line?", "GPT is modern 64-bit tables; MBR is legacy with a 2TiB-ish cap and four primary slots.", ["lvm-and-fstab"], "The professional disk skill is restraint plus lsblk."),
    cx("uuid-not-sda", "UUID in fstab, not /dev/sda", "Device names reorder. UUID stays with the filesystem. blkid prints them.", "PARTUUID is the partition table id; UUID is the filesystem id. Do not mix them up.", "A passport number versus a seat number that changes every flight.", "blkid | head", "UUID=\"...\" TYPE=\"ext4\"", "Copy from here into fstab.", "Why did / mount the wrong disk after adding a volume?", "fstab used /dev/sdb and names shifted. Use UUID.", ["mounts"], "Stable names: UUID, /dev/disk/by-uuid, or by-label."),
  ],
  "lvm-and-fstab": [
    cx("df-then-du", "df is the gauge; du is the compartments", "df -hT: which mount is full. df -i: inodes. du -xhd1: biggest directory on that filesystem only.", "Deleted-open files: df high, du low. Then lsof.", "Fuel gauge vs opening the trunk.", "df -hT | head", "Filesystem Type Size Used", "Always pair with df -i when create-file fails.", "50% df but cannot create files?", "Inodes, quotas, read-only remount, or permissions — not always 'disk full'.", ["performance-basics"], "Measure the mount, then the directory, then open files."),
    cx("grow-lv", "Growing a disk is three steps", "Cloud volume grow → grow partition/PV → lvextend → grow the filesystem (resize2fs or xfs_growfs). Missing the last step leaves df unchanged.", "XFS cannot shrink. Snapshots first.", "Lengthening a hose, then telling the water the hose is longer.", "man lvextend | head", "LVEXTEND(8)", "Read the man page on the actual host.", "Why df unchanged after lvextend?", "Filesystem not grown.", ["disks-partitions"], "Block device, LVM, filesystem — three layers, three commands."),
  ],
  "linux-security": [
    cx("selinux-not-off", "Do not setenforce 0 as a fix", "SELinux denials look like Permission denied. Read the AVC (ausearch, journal) and add a policy or fix the context. Permanent Permissive hides bugs.", "Ubuntu often AppArmor instead. ls -Z shows labels on SELinux hosts.", "Building rules that apply even to the owner.", "getenforce 2>/dev/null || echo 'no getenforce — check aa-status'", "Enforcing", "Enforcing is the goal on RHEL-like prod.", "AVC?", "Access Vector Cache denial — SELinux said no. It is a log line, not a virus.", ["production-linux"], "Treat MAC denials as data. Disabling the MAC is an incident of its own."),
    cx("least-privilege", "Least privilege is a habit", "Separate users, no shared root SSH, firewall allowlists, secrets 600, no 777, no secret in git.", "Capabilities split root. seccomp reduces syscalls. That is how containers harden.", "Only the keys you need today.", "id", "uid=1000(student) ...", "You should rarely need uid 0 on a laptop lesson — and even less on a server.", "sudo chmod 777 to make an app work?", "Never as policy. Fix owner, group, and mode; then MAC.", ["users-groups"], "Every 777 and every shared root key is a future incident."),
  ],
  "performance-basics": [
    cx("load-not-percent", "Load average is not CPU%", "It is a queue length (runnable + uninterruptible), averaged. Compare to CPU count. Load 32 on 4 vCPUs is saturation.", "vmstat r and wa split CPU vs disk. First vmstat line is since boot — ignore it.", "How many people are in line, not how loud the cashier is.", "uptime; vmstat 1 3", "load average: ...", "Sample lines after the first.", "High load, idle CPU?", "Often D-state I/O or NFS. iostat and dmesg next.", ["memory-ipc"], "USE method: utilization, saturation, errors — then change one thing."),
    cx("cache-is-not-waste", "Linux uses free RAM for cache", "free's available is what you can still allocate. Huge 'used' that is cache is healthy. Dropping caches is a last-resort test, not a tune.", "OOM killer fires when reclaim fails. Watch dmesg for oom-kill.", "Books left open on a table — still reusable.", "free -h", "Mem: ... available ...", "Read available, not used.", "Should you disable swap?", "On tiny VMs it can prevent death-by-thrash or cause OOM sooner. Know the tradeoff; do not cargo-cult.", ["memory-ipc"], "Cache is working as designed. Panic at low available plus paging, not at a full cache."),
  ],
  "tcp-and-sockets": [
    cx("time-wait", "TIME-WAIT is not a leak", "A closed TCP connection waits so delayed packets do not confuse a new connection. Huge counts can exhaust ephemeral ports under load tests.", "ss -tan | grep TIME. Tuning tw reuse is a last resort behind NAT.", "Lingering at the counter so a late letter is not misread.", "ss -tan | head", "State ...", "Count ESTAB vs TIME-WAIT during tests.", "Connection refused vs timeout?", "Refused: RST, reachable host, no listener. Timeout: drop, routing, or filter.", ["net-fundamentals"], "Sockets are file descriptors. ulimit -n matters under load."),
    cx("ephemeral", "Ephemeral ports", "Outbound connections pick a high local port. Exhaustion looks like 'cannot connect' under load. ss + nstat help.", "ip_local_port_range sysctl. Connection pooling reduces churn.", "Temporary ticket numbers at a deli.", "sysctl net.ipv4.ip_local_port_range", "32768 60999", "Typical Linux default range.", "TIME-WAIT pile + port exhaustion?", "Reuse connections, scale out, or (carefully) tune. Measure first.", ["performance-basics"], "Load-test with ss open. Port exhaustion is a real production bug."),
  ],
  "namespaces-cgroups": [
    cx("not-a-vm", "A container is not a VM", "Same kernel. Isolated view (namespaces) and meters (cgroups). PID 1 inside is not host systemd. Privileged containers punch holes.", "User namespaces map UIDs. Overlay FS is the layered disk illusion.", "Different windows on the same city, plus an electricity meter.", "lsns | head", "NS TYPE NPROCS ...", "In a container the PID ns is short.", "Why Docker on Mac is slower?", "It runs a Linux VM. Namespaces are a Linux kernel feature.", ["memory-ipc"], "If you understand namespaces + cgroups, Kubernetes questions get easier."),
    cx("cgroup-limit", "When the app is 'slow' check the quota", "CPU and memory limits in cgroups look like a weak machine. docker stats / kubectl top. OOMKill is a cgroup event too.", "cgroup v2 is a unified tree. systemd slices are cgroups.", "A breaker panel for each apartment.", "man cgroups | head", "CGROUPS(7)", "Worth one slow read.", "throttled CPU?", "cfs quota. The host may be idle while the container is capped.", ["performance-basics"], "Limits are features. They also create fake 'hardware' symptoms."),
  ],
  "memory-ipc": [
    cx("page-cache", "Page cache in one sentence", "RAM remembers file contents so the next read skips disk. Clean pages can be dropped instantly; dirty pages must write back first.", "vmtouch and /proc/meminfo Cached. fsync forces durability.", "Keeping the cookbook open on the counter.", "grep -E 'Cached|Dirty|MemAvailable' /proc/meminfo", "Cached: ...", "Numbers in kB.", "Why shutdown is slow?", "Dirty writeback. Lots of recent writes.", ["performance-basics"], "Free RAM that becomes cache is a win, not a leak."),
    cx("ulimit-files", "Too many open files", "Each socket and file is an fd. ulimit -n is the cap. nginx and databases hit this first.", "systemd LimitNOFILE=. The hard limit is a ceiling.", "A handful of tickets you are allowed to hold.", "ulimit -n", "1024", "Many servers raise this on purpose.", "EMFILE?", "Process hit its fd limit. Count /proc/PID/fd and check leaks.", ["tcp-and-sockets"], "Sockets are files. Treat fd limits as capacity planning."),
  ],
  "unix-posix": [
    cx("gnu-flags-on-mac", "macOS is Unix, not GNU", "ls, find, and sed flags differ. Homebrew coreutils install gtimeout etc. systemd and /proc are absent.", "POSIX is the overlap. Test portable scripts on dash.", "Same language family, different slang.", "uname -s", "Linux  (here) or Darwin (Mac)", "Scripts should check or stick to POSIX.", "ps aux on Mac?", "Works (BSD). Linux ps also takes -ef. AIX may not like aux.", ["linux-vs-unix"], "Write POSIX first. Document GNU extensions in the README."),
    cx("sbin-admin", "Why some commands live in sbin", "Traditionally admin tools (fsck, ip) were in sbin and not on a user PATH. Merged /usr blurs this. Still appears in interviews.", "Need root for the action, not always to see the binary.", "A locked tools closet.", "command -v ip || ls /usr/sbin/ip", "/usr/sbin/ip", "May need to prepend /usr/sbin on some Unixes.", "Why 'command not found' as a user but root works?", "root PATH includes sbin.", ["env-and-path"], "Check PATH and sbin before reinstalling the package."),
  ],
  "production-linux": [
    cx("reversible-change", "Every change needs an undo", "Deploy is not done until rollback is typed. systemd, images, and config management exist so you can reverse.", "Pet servers without backups are hopes, not plans.", "A restaurant ticket you can void.", "systemctl is-system-running 2>/dev/null || echo 'check PID 1'", "running", "degraded means failed units.", "Sev1 first minute?", "Stop bleeding, communicate, preserve evidence, then root-cause. Do not chmod 777.", ["linux-security"], "Production is Linux plus change control, observability, and backups."),
    cx("immutable-idea", "Cattle, not pets", "Replace VMs/images instead of SSHing a unique snowflake. SSH still exists for break-glass.", "Cloud-init, golden images, Kubernetes pods. The kernel skills stay the same.", "A rental car you do not customize with stickers.", "echo 'this host should be reproducible'", "this host should be reproducible", "If you cannot rebuild it, you do not own it.", "Why still learn SSH and journalctl?", "Break-glass and because the thing you orchestrate is still Linux.", ["namespaces-cgroups"], "Automate the boring path. Keep the kernel skills for the 3 a.m. path."),
  ],
};
